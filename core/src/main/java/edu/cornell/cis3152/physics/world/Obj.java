package edu.cornell.cis3152.physics.world;

/**
 * Photographable object types in Frogtographer.
 *
 * <p>Each constant corresponds to a kind of subject the player can capture; gameplay maps
 * each type to a {@link Quality} and an {@link ObjectEffect}
 * strategy that determines how transferred surface effects behave.
 */
public enum Obj {
    HONEY,
    CLOUD,
    ICE
}
